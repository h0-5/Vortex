services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: vortex
      POSTGRES_USER: vortex
      POSTGRES_PASSWORD: vortex
    ports:
      - '5432:5432'
    volumes:
      - vortex-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U vortex -d vortex']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  vortex-postgres:
